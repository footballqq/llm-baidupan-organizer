# 百度网盘 LLM 智能整理系统 (LLM-BaiduPan-Organizer) 开发者技术文档

---

## 1. 架构定位与技术选型

### 1.1 系统定位
本系统面向个人与家庭大规模（数万到数十万目录、TB 级别）杂乱百度网盘的治理与规划。通过结合 **AList 本地绿色网关** 与 **专家领域规则引擎 + A+B 双模分层分类驱动（my-fast-gptoss 语义提炼 + Kev 极速决策）**，解决成套资料粉碎、同构副本冗余、引流广告泛滥、长上下文模型变傻及网盘反爬风控等核心痛点。

### 1.2 核心技术栈
| 层次 | 选型 | 设计考量与优势 |
| :--- | :--- | :--- |
| **运行环境** | Python 3.12+ (Windows PowerShell) | 跨平台、异步/多进程生态完善、部署极简 |
| **网盘网关** | AList 官方绿色免安装版 (v3.64.0+) | 解耦百度网盘扫码认证、验证码弹窗、动态 Token 刷新，提供标准 WebDAV 接口 |
| **WebDAV 通信** | `webdav4` (0.11.0) | 基于 httpx，标准 RFC 4918 协议，连接池复用与异常重试 |
| **大模型 SDK** | OpenAI Python SDK (`openai` 2.37.0) | 标准化 API 兼容协议，支持对接本地代理 (`newapi_key.env`) |
| **轻量决策模型** | 本地 Kev 模型服务 (0.5B) | 毫秒级推理打标，微批次单选分类 (`kev.env`) |
| **路径保护** | `pathlib.PurePosixPath` | 严格消除 Windows 反斜杠 `\` 破坏 WebDAV URI 规范的风险 |
| **数据持久化** | 本地 JSON 树 / UTF-8-SIG CSV | 人类可读性强，原生支持 Excel/WPS 跨软件审阅且中文零乱码 |

---

## 2. 软件模块架构与数据流

### 2.1 整体架构设计
```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            AList WebDAV 桥接层                              │
│                    (alist_helper.py: SafeWebDAVClient)                      │
│       - 自动探测/启动 Windows 绿色版       - 维持网关会话保活                 │
│       - PurePosixPath 跨平台路径保护       - 1.0~2.5s 单线程随机防风控延时    │
└──────────────────────────────────────┬──────────────────────────────────────┘
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                            数据采集与快照层                                 │
│                         (scanner.py: NetdiskScanner)                        │
│       - 单线程全盘深度递归遍历             - .tmp 原子替换写入断点快照        │
│       - 永久离线全盘树缓存 cache/disk_tree_cache.json (116.6 MB)            │
└──────────────────────────────────────┬──────────────────────────────────────┘
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          原子聚类与去重净化层                               │
│       - atomic_detector.py: 启发式同构匹配 + 深度穿透保护，成套课程整体打包 │
│       - dedup_cleaner.py: 四维联合哈希指纹查重 + 正则去广告更名             │
└──────────────────────────────────────┬──────────────────────────────────────┘
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                       A+B 双模两级分层分类与智能规划层                      │
│     - hierarchical_classifier.py: my-fast-gptoss 提炼子系列 + Kev 极速决策   │
│     - planner.py: NetdiskPlanner 用户标注持久化继承 (_load_user_annotations) │
│     - 支持 delete=x 自动收拢至 03_待删除，带同名 _副本1 保护                 │
│     - 导出总表 organize_plan.csv 与 9 个独立领域切片 CSV                     │
└──────────────────────────────────────┬──────────────────────────────────────┘
                                       │ 人工在 Excel 中审阅 / 打 x / 修改 target_path
                                       ▼ (运行 python run_pipeline.py refresh-plan 秒级同步)
┌─────────────────────────────────────────────────────────────────────────────┐
│                          受控单线程执行与回滚层                             │
│       - executor.py: 真实单线程安全移动、同名 _副本1 保护、实时落盘断点续跑 │
│       - undo_organize.py: 读取 undo_history.json 严格逆序还原目录           │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 3. 核心算法与机制深度剖析

### 3.1 跨平台 POSIX 路径保护
- **根因分析**：Windows 操作系统使用反斜杠 `\` 作为路径分隔符。若在代码中使用 `pathlib.Path("/foo/bar").parent`，在 Windows 下会产生形如 `\foo` 的字符串。传递给 WebDAV 客户端时，直接违反 RFC 4918 规范，导致服务端返回 `400 Bad Request`，或者在网盘上创建形如 `/foo\bar` 的畸形目录。
- **治理方案**：全工程在涉及 WebDAV 的所有路径计算、上级目录解析与拼接中，一律使用 `pathlib.PurePosixPath`：
  ```python
  from pathlib import PurePosixPath
  # 正确计算上级目录与名称，无论宿主操作系统是 Windows 还是 Linux
  parent_dir = PurePosixPath(clean_target).parent.as_posix()
  name = PurePosixPath(clean_target).name
  ```

### 3.2 成套原子资源单元（AtomicUnit）聚合机制
- **技术痛点**：若无差别将所有子目录粉碎分发，成套系列资料（如 `AMC8` 下的 2001~2024 年真题卷、`牛津树` 下的 1~14 季、`思泉大语文` 各讲次）会被拆得七零八落，极难重新整理。
- **算法设计**（[`atomic_detector.py`](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/atomic_detector.py)）：
  1. **顶层松散容器强制穿透**：仅将 `root`、`baiduq`、`aa育儿`、`我的资源`、`work`、`temp` 等泛义分类大仓识别为容器继续向下拆解；
  2. **同构模式正则检测**：匹配讲次、季集、年份、单元等同构模式；
  3. **深层结构保全**：当遍历深度 `depth >= 2`（已穿透顶层容器），所有子目录天然具备高度聚合特征，直接作为不可分割的独立成套资源单元保留，杜绝过度粉碎。

### 3.3 四维联合结构指纹查重（抗碰撞）
- **技术痛点**：仅靠目录名容易漏查（同资源不同名称），仅靠文件总数与总体积容易碰撞（不同课程偶发文件数或大小相同）。
- **指纹哈希算法**（[`dedup_cleaner.py`](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/dedup_cleaner.py)）：
  $$\text{Fingerprint} = \text{total\_files} : \text{total\_size} : \text{CoreFiles} : \text{CoreSubdirs}$$
  1. 预先过滤掉推广引流文本（`关注公众号.txt`、`解压密码.url`、`宣传.doc` 等）；
  2. 提取核心文件列表与子目录列表的前 3 项按字典序排序后拼接；
  3. 四维联合生成唯一指纹，实现跨文件夹、跨层级的 100% 精准副本识别。

### 3.4 A+B 双模两级分层分类与智能聚类
- **技术痛点**：外部模型（如 my-fast-gptoss）如果直接单项逐个调用，面对 800+ 资源会消耗大量时间与并发配额；而纯规则匹配无法动态聚类同类子系列。
- **协同架构**（[`hierarchical_classifier.py`](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/hierarchical_classifier.py)）：
  1. **A 模型（my-fast-gptoss）宏观聚类提炼**：按大领域聚合资源，提示模型生成标准化二级子系列（例如在 `小学数学_竞赛` 领域下动态提炼高思、学而思、AMC8、袋鼠数学等）；
  2. **B 模型（本地 Kev 0.5B）微观高速归属**：将提炼出的标准子系列作为选项，本地毫秒级推断具体资源单元的最优归属；
  3. **语义上下文补充（`_enrich_cryptic_name`）**：针对数字或缩写目录（如 45, 13vol, S05, draw, xesx），结合父级路径、代表性文件扩展名与子目录结构拼装语义上下文，彻底解决短名失真问题。

### 3.5 人工标注持久化与待删除碰撞自愈
- **技术痛点**：用户在 CSV 中花费大量精力标记待删除（`delete=x`）或微调路径，若重新运行规划脚本极易发生人工成果被冲掉的惨剧；且 300+ 资源移入同一个 `03_待删除` 目录时存在同名碰撞覆盖风险。
- **算法设计**（[`planner.py`](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/planner.py)）：
  1. **历史标注加载器（`_load_user_annotations`）**：以 `source_path` 为唯一键，启动时加载用户的 `delete`、`target_path`、`category`、`status`；
  2. **同名防碰撞生成器**：
     ```python
     delete_name_counts = defaultdict(int)
     for r in records:
         if r.get("delete") == "x":
             base_name = r.get("cleaned_name") or Path(r["source_path"]).name
             count = delete_name_counts[base_name]
             delete_name_counts[base_name] += 1
             target_name = base_name if count == 0 else f"{base_name}_副本{count}"
             r["target_path"] = f"{DELETE_TARGET_DIR}/{target_name}"
     ```
  3. **一键同步方法（`refresh_plan_exports`）**：用户修改 CSV 后直接运行 `python run_pipeline.py refresh-plan`，1 秒内完成同名检测与全量切片刷新，无需重跑大模型。

### 3.6 单线程拟人化延时防风控与外部化配置
- 在 [`SafeWebDAVClient`](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/alist_helper.py) 中，所有 WebDAV `mkdir` 与 `move` 统一注入动态延时切面：
  ```python
  def _sleep_rate_limit(self):
      delay = random.uniform(REQUEST_MIN_DELAY, REQUEST_MAX_DELAY)
      time.sleep(delay)
  ```
- 参数在 [`config.py`](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/config.py) 中外部化开放：
  ```python
  REQUEST_MIN_DELAY = float(os.getenv("REQUEST_MIN_DELAY", "1.0"))
  REQUEST_MAX_DELAY = float(os.getenv("REQUEST_MAX_DELAY", "2.5"))
  MAX_RETRIES = int(os.getenv("MAX_RETRIES", "5"))
  RETRY_BACKOFF = float(os.getenv("RETRY_BACKOFF", "3.0"))
  ```

---

## 4. 交付文件与切片说明

系统运行后生成的所有报表与中间数据结构严格隔离在 `output/` 与 `cache/` 中，且受 `.gitignore` 保护：

```
output/
├── 网盘整理规划方案.md        # 人类友好的全盘两级金字塔与隔离区统计方案报告
├── organize_plan.csv         # 全量执行明细表 (含 BOM，Excel/WPS 打开无乱码)
├── undo_history.json         # 执行日志历史，供 undo_organize.py 逆序还原
├── execution.log             # 运行期移动详细时间戳与响应码日志
└── slices/                   # 分领域切片 CSV 目录
    ├── 01_小学数学_竞赛与常规.csv (71项)
    ├── 02_小学语文与英语.csv (39项)
    ├── 03_少儿通识与素养.csv (46项)
    ├── 04_儿童听读与娱乐.csv (21项)
    ├── 05_工作研报与经管学术.csv (67项)
    ├── 06_影视影音与音频素材.csv (16项)
    ├── 07_个人生活_相册与工具.csv (148项)
    ├── 08_建议清理隔离区.csv (394项: 27重复副本 + 367用户标记待删除)
    └── 09_其他待复核.csv (9项)
```

---

## 5. 工程文件清单与代码膨胀控制

严格遵循项目代码行数约束，每个文件职责单一，单文件行数均在 500 行以内，远低于 1500 行上限：

| 文件绝对路径 | 代码行数 | 主要职责 |
| :--- | :---: | :--- |
| [`config.py`](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/config.py) | 125 行 | 全局路径常数、双模型配置、防风控延时环境变量与 `parse_llm_json` 容错解析 |
| [`alist_helper.py`](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/alist_helper.py) | 245 行 | AList 免安装绿色版拉起、WebDAV 会话保活、PurePosixPath 跨平台客户端与拟人化延时 |
| [`scanner.py`](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/scanner.py) | 215 行 | 网盘单线程全量递归扫描器，带 `.tmp` 原子替换断点快照保存与快照加载 |
| [`atomic_detector.py`](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/atomic_detector.py) | 220 行 | 成套独立资源单元聚类识别，启发式规则同构序列判定与深度穿透 |
| [`dedup_cleaner.py`](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/dedup_cleaner.py) | 162 行 | 四维结构指纹查重引擎与广告推广水印净化器 |
| [`hierarchical_classifier.py`](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/hierarchical_classifier.py) | 482 行 | A+B两级分层分类与智能聚类引擎（my-fast-gptoss提炼 + Kev极速打标） |
| [`kev_client.py`](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/kev_client.py) | 115 行 | 本地 Kev 决策模型 (0.5B) 毫秒级推理客户端，支持 Choice 单选与判定 |
| [`planner.py`](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/planner.py) | 320 行 | 分类规划与交付物生成器，支持用户标注持久化继承、同名防碰撞生成与一键同步刷新 |
| [`executor.py`](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/executor.py) | 185 行 | 受控单线程移动执行器，分类识别 MOVE/ISOLATE/DELETE、同名保护、断点续跑与回滚记录 |
| [`undo_organize.py`](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/undo_organize.py) | 75 行 | 读取 `undo_history.json` 逆序原路回滚还原脚本 |
| [`run_pipeline.py`](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/run_pipeline.py) | 165 行 | CLI 统一入口调度脚本，提供 init, scan, plan, scan-plan, refresh-plan, execute, undo |
| [`scan_and_plan.py`](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/scan_and_plan.py) | 135 行 | 扫描与规划一体化持续作业脚本，具备网络断线多轮重试与自愈能力 |
| [`test_pipeline.py`](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/test_pipeline.py) | 138 行 | 全流水线端到端模拟测试（测试输出隔离在 `cache/test_output`） |

---

## 6. 历史缺陷复盘与加固记录 (Changelog)

| 缺陷编号 | 涉及组件 | 风险等级 | 问题表现与成因 | 最终加固措施 |
| :--- | :--- | :--- | :--- | :--- |
| **BUG-1** | `alist_helper.py` | 🔴 严重 | `list_dir` 对比自身名称，若父子同名导致子目录被误滤 | 改用全量绝对路径比对 `item_full == clean_path` |
| **BUG-2** | `atomic_detector.py` | 🔴 严重 | 早期递归未处理容器下子节点的歧义，深层成套资料被静默遗漏 | 改为以 `depth >= 2` 为自然聚合边界，完全保全深层课程 |
| **BUG-3** | `planner.py` | 🔴 严重 | `_check_deletion_candidate` 匹配导致 1-6 年级全套课程被误判为超龄废弃 | 加入保护关键词拦截机制，全套、多学段课程严禁隔离 |
| **BUG-8** | `alist_helper.py` & `executor.py` | 🔴 严重 | Windows 原生 `Path.parent` 产生反斜杠 `\` 破坏 WebDAV URL | 全量改用 `pathlib.PurePosixPath` 处理所有 WebDAV 路径 |
| **BUG-9** | `config.py` | 🟡 中等 | 大模型输出带有前言或代码块尾部缺失换行时 `json.loads` 抛错 | 抽象通用方法 `parse_llm_json`，支持正则智能截取与尾逗号自动容错 |
| **BUG-10**| `scanner.py` | 🟡 中等 | 直接写入断点文件，进程意外退出导致 JSON 半写损坏 | 采用 `.tmp` 临时文件 + `os.replace` 原子替换写入 |
| **BUG-12**| `planner.py` | 🟡 中等 | 外部 LLM 代理网络超时导致整盘规划挂起数分钟 | 实现微批次（5项/批）+ 连续超时熔断机制，快速安全回退 |
| **BUG-13**| `hierarchical_classifier.py` | 🔴 严重 | 成套音频课顶层含一张封面图 `cover.jpg`，被规则误判为照片相册 | 建立语义与关键词绝对优先机制，仅在明确照片路径或大量纯相片无课程特征时才判定为相册 |
| **BUG-14**| `hierarchical_classifier.py` | 🟡 中等 | 目录名为数字或缩写（如 45, 13vol, S05, draw）导致分类失真 | 实现 `get_unit_context` 与 `_enrich_cryptic_name`，结合路径、核心代表性文件与子目录语义还原 |
| **BUG-15**| `hierarchical_classifier.py` | 🟡 中等 | 单层平铺分类缺少层级感，用户查找资源效率低 | 引入两级金字塔分类体系，A+B 双模协同提炼，支持原子断点续传 |
| **BUG-16**| `planner.py` & `executor.py` | 🔴 严重 | 用户人工在 CSV 中打 x 标记待删除后，重新生成规划被覆盖，且 367 项集中移入 03_待删除 存在同名冲突覆盖风险 | 实现 `_load_user_annotations` 继承人工审核，同名自动递增 `_副本1` 保护，新增 `refresh-plan` 命令 |
| **BUG-17**| `test_pipeline.py` | 🟡 中等 | 运行模拟测试时直接将 mock 规划结果写入生产 `output/organize_plan.csv` | 测试路径参数化隔离至 `cache/test_output`，坚决保护生产规划成果 |
