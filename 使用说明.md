# 百度网盘 LLM 智能整理系统 (LLM-BaiduPan-Organizer) 使用说明书

---

## 一、 系统定位与整体架构

针对百度网盘存储海量（TB 级别、数十万文件）、层级深度嵌套、广告水印泛滥、以及大模型在超长上下文中容易失效的问题，本系统采用 **“免安装本地网关 (AList) + 高精度规则前置 + A+B 双模分层分类 (my-fast-gptoss 语义提炼 + Kev 极速决策) + 结构指纹去重 + 受控单线程拟人化执行”** 的高安全架构。

```
┌────────────────┐      扫码授权      ┌────────────────┐
│   百度网盘官方   │ ◄────────────────► │ AList 本地网关 │
└────────────────┘                    └───────┬────────┘
                                              │ 标准 WebDAV (PurePosixPath 跨平台保护)
                                              ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                       Python 核心作业流水线                                  │
│                                                                             │
│  1. 深度遍历扫描：单线程拟人化随机延时 (1.0~2.5s) + 原子双缓存断点续扫        │
│  2. 独立成套聚合：成套课程 (如高思/AMC8/Raz/思泉) 整体保留，顶层容器拆解分发 │
│  3. 混合指纹查重：文件数:体积:核心文件:子目录联合防碰撞，自动检出重复副本     │
│  4. 广告水印净化：正则速洗去公众号水印 + 必要时大模型学术规范命名           │
│  5. 两级金字塔归类：A+B 协同 (大模型提炼子系列 + 本地 Kev 0.5B 极速匹配)    │
│  6. 9大领域切片导出：生成分领域 CSV，避免大表卡顿，方便在 Excel 中分页审阅    │
│  7. 用户审核与delete标记：表格打 x 自动收拢至 03_待删除，自动防碰撞重命名    │
│  8. 安全受控移动：同名 _副本1 递增防覆盖 + 100% 隔离区软删除 + 一键原路撤销  │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 核心安全保障与避坑设计
1. **绝对零物理删除（100% 软隔离安全存储）**：
   - 系统内部**绝对不执行任何物理硬删除**；
   - 所有的超龄资源、查重检测出的重复副本，以及用户在表格中打 `x` 标记待删除的资源，全部统一移入网盘根目录的 **`/_待清理隔离区/`**：
     - `/_待清理隔离区/01_确凿重复副本/`（严格哈希查重确认的冗余副本，27 项）
     - `/_待清理隔离区/03_待删除/`（用户在表格中标记 `[delete=x]` 的各类待清理资源，367 项）
   - 用户可以在网盘客户端随时二次核实或一键还原，绝无数据丢失风险。
2. **待删除资源同名碰撞自动防护**：
   - 待删除资源中若存在同名目录（如多份 `公文数学全套`、`巧虎` 等），系统在移动至待删除目录时会自动追加 `_副本1`、`_副本2` 后缀，确保 100% 互不覆盖。
3. **人工标注持久化与防覆盖保护**：
   - 用户在 CSV 中花费时间标记的 `delete` 标志、自定义修改的 `target_path` 以及 `SKIP` 跳过状态均受持久化保护；后续即使重新生成方案，系统也会**自动继承并保留您的全部审核成果**。
4. **四重防风控与反限流保护**：
   - **严格单线程**：无任何多线程并发，避免突发高频冲击网盘 API；
   - **拟人化随机延时**：每次操作前强制休眠 **1.0 ~ 2.5 秒** 随机时间（可通过环境变量自由调节），消除机器固定特征；
   - **指数退避重试**：遇到网络异常或限流时自动按 3s -> 6s -> 9s -> 12s -> 15s 退避等待，最多重试 5 次；
   - **单步实时落盘**：每成功移动一项立即写回 CSV 为 `COMPLETED`，支持随时中断、断点续传。
5. **一键原路逆序回滚（Undo）**：
   - 所有移动操作均实时记录至 `output/undo_history.json`，随时可通过单条命令按逆序将文件原路退回原始位置。

---

## 二、 目标归档两级金字塔体系与切片说明

系统根据小学三年级孩子学习备考、儿童娱乐听读、成人投研工作、家庭相册备份等多角色需求，设计了规范的两级层级结构：

### 1. 全盘 811 项资源分布概览

| 任务分类 | 数量 | 目标路径与处理方式 | 说明 |
| :--- | :---: | :--- | :--- |
| **两级规范移动项** | **417 项** | 金字塔大纲（一级领域 / 二级细分子系列 / 资源目录） | 保留的核心学科与工作生活资源 |
| **确凿重复副本** | **27 项** | `/_待清理隔离区/01_确凿重复副本/` | 与正本文件数与体积完全一致的冗余副本 |
| **用户标记待删除** | **367 项** | `/_待清理隔离区/03_待删除/` | 用户在表格中标记 `[delete=x]` 的废弃资料 |
| **全盘合计** | **811 项** | — | **已全量通过仿真预览验证** |

### 2. 9 大分领域切片表格清单

为了方便在 Excel 或 WPS 中审阅，避免在数百行的大表里眼花缭乱，系统在 [output/slices/](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/output/slices/) 目录下生成了 9 个独立的切片 CSV 表（已包含 UTF-8-SIG，双击直接打开无乱码）：

| 切片文件名 | 数量 | 包含核心内容 |
| :--- | :---: | :--- |
| [01_小学数学_竞赛与常规.csv](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/output/slices/01_小学数学_竞赛与常规.csv) | 71 项 | AMC8真题、高思导引/课本、学而思大白本、袋鼠竞赛、迎春杯、公文数学、口算速算 |
| [02_小学语文与英语.csv](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/output/slices/02_小学语文与英语.csv) | 39 项 | 思泉大语文全套、窦神诸葛学堂、好字在、文言文古诗词、牛津树1-14、Raz原版分级、KET/PET真题 |
| [03_少儿通识与素养.csv](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/output/slices/03_少儿通识与素养.csv) | 46 项 | 少儿编程(Scratch/Python)、科普百科、趣味历史地理、艺术绘画与书法、家庭教育指南 |
| [04_儿童听读与娱乐.csv](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/output/slices/04_儿童听读与娱乐.csv) | 21 项 | 凯叔讲故事系列(西游记/三国等)、钱儿爸系列、名著评书曲艺、原版少儿听读动画 |
| [05_工作研报与经管学术.csv](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/output/slices/05_工作研报与经管学术.csv) | 67 项 | 量化与金工多因子(Alpha101/GTJA191)、券商研报、期权高频策略、经管社科精读 |
| [06_影视影音与音频素材.csv](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/output/slices/06_影视影音与音频素材.csv) | 16 项 | 经典美剧与剧集、高清电影与纪录片、音乐音频与音效素材库 |
| [07_个人生活_相册与工具.csv](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/output/slices/07_个人生活_相册与工具.csv) | 148 项 | 家庭照片相册备份、系统装机软件与常用工具备份 |
| [08_建议清理隔离区.csv](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/output/slices/08_建议清理隔离区.csv) | 394 项 | **重点清理表**：包含 27 项确凿重复副本 + 367 项用户打 x 待删除资源 |
| [09_其他待复核.csv](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/output/slices/09_其他待复核.csv) | 9 项 | 少数纯数字或特殊命名待复核资源 |

> [!NOTE]
> 核心学科/工作切片（`01` 至 `07` 及 `09`）已**自动过滤剔除打 `x` 的待删除项**，打开学科切片看到的都是干净整洁的保留资源。

---

## 三、 标准操作流程（全流水线）

所有命令均在项目目录（`E:\users\kpan\BaiduSyncdisk\program\aigc\utils\baiduwangpan`）下的 **Windows PowerShell** 中运行：

### 步骤 1：初始化与启动 AList 网关
```powershell
python run_pipeline.py init
```
- 自动准备免安装绿色版 `alist_bin/alist.exe` 并在后台安全启动服务；
- 浏览器访问控制台：[http://localhost:5244](http://localhost:5244)
  - 账号：`admin`，密码：`admin`
  - 若首次使用，点击【管理】->【存储】->【添加】，驱动选择【百度网盘】，挂载路径填写 `/baiduq`，使用百度网盘手机 App 扫码登录即可。

---

### 步骤 2：全盘安全扫描（单线程随机延时 + 断点续扫）
```powershell
python run_pipeline.py scan
```
- 本地若已存在 `cache/disk_tree_cache.json` 快照，系统会提示直接复用（输入 `Y` 秒级载入）；
- 如需强制重新全盘遍历扫描网盘，可附加 `--force` 参数：
  ```powershell
  python run_pipeline.py scan --force
  ```

---

### 步骤 3：智能规划生成
```powershell
python run_pipeline.py plan
```
- 运行原子聚类、四维哈希去重、广告净化与 A+B 双模两级智能分类；
- 运行完毕后输出三大交付物：
  - 方案总结报告：[output/网盘整理规划方案.md](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/output/%E7%BD%91%E7%9B%98%E6%95%B4%E7%90%86%E8%A7%84%E5%88%92%E6%96%B9%E6%A1%88.md)
  - 全量执行总表：[output/organize_plan.csv](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/output/organize_plan.csv)
  - 9 大领域切片表：[output/slices/](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/output/slices/)

---

### 步骤 4：人工审核、打 x 标记与一键刷新

您可以在 Excel 或 WPS 中直接打开 [output/organize_plan.csv](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/output/organize_plan.csv) 或 [output/slices/](file:///E:/users/kpan/BaiduSyncdisk/program/aigc/utils/baiduwangpan/output/slices/) 下的任意切片表格进行审阅与微调：

1. **标记移入待删除目录**：
   - 在任意行的 **`delete` 列填入 `x`**（大小写均可）；
2. **微调目标归档路径**：
   - 若对某个资源的二级子系列归类不满意，可直接修改 **`target_path`** 列；
3. **临时跳过某项不动**：
   - 将对应行的 **`status`** 列修改为 **`SKIP`**；
4. **一键同步刷新（无需重跑大模型）**：
   - 保存修改后的 CSV 文件，在 PowerShell 中执行以下命令，系统将在 1 秒内自动完成同名防碰撞计算，并同步刷新切片表与报告：
     ```powershell
     python run_pipeline.py refresh-plan
     ```

---

### 步骤 5：仿真预览与安全受控执行

#### 1. 无风险仿真预览（Dry Run）
在真实移动网盘文件前，建议先执行仿真预览，验证全部 811 项的路径有效性及冲突处理：
```powershell
python run_pipeline.py execute --dry-run
```
> [!NOTE]
> 仿真预览不会对网盘做任何物理变更，仅模拟演练所有 `MOVE`、`ISOLATE` 和 `DELETE` 任务。

#### 2. 正式受控单线程执行
核对确认无误后，启动真实执行：
```powershell
python run_pipeline.py execute
```
- **单线程拟人化延时**：严格保持 1.0 ~ 2.5 秒随机延时逐项移动，安全防风控；
- **同名防覆盖**：若目标路径已存在同名目录，自动重命名为 `_副本1`、`_副本2`；
- **实时落盘与断点续跑**：每完成 1 项立即标记为 `COMPLETED`，中途随时可按 `Ctrl + C` 暂停，下次执行自动断点跳过已完成项；
- **操作全程追溯**：移动记录实时追加至 `output/undo_history.json` 与 `output/execution.log`。

#### 3. 可选：进一步放宽延时（更稳妥）
若网盘账号等级较低或希望以更稳节奏隔夜运行，可通过环境变量自由调节随机延时区间：
```powershell
$env:REQUEST_MIN_DELAY = "2.0"
$env:REQUEST_MAX_DELAY = "4.0"
python run_pipeline.py execute
```

---

## 四、 紧急一键原路回滚（Undo）

若执行后因任何原因希望撤回操作、恢复原状，只需在终端中运行：
```powershell
python run_pipeline.py undo
```
系统将读取 `output/undo_history.json`，严格以执行顺序的**逆序**将每一个文件夹原路移动回原来的位置，确保零资产错位。

---

## 五、 常见问题排查与 FAQ

**Q1：如何确认 AList 网关正常就绪？**
> 浏览器打开 [http://localhost:5244](http://localhost:5244)，若能在 `/baiduq` 路径下正常浏览百度网盘文件结构，即代表网关处于正常连接状态。

**Q2：如果执行到一半断网或被意外关闭了怎么办？**
> 本系统具备单步落盘与断点自愈保护：直接重新运行 `python run_pipeline.py execute`，系统会自动识别历史已完成项（`COMPLETED`）并秒级跳过，仅继续执行剩余未完成的任务项。

**Q3：移动速度为什么保持单线程随机延时？**
> 百度网盘针对连续高频机器请求有触发式限流风控。单线程 + 1.0~2.5 秒随机延时是保证数百 GB 到数 TB 海量资料“零风控、零封号、平稳完成”的最优工程解。整理任务在后台静默运行即可。

**Q4：如果我在 CSV 中把打 x 的项删除了，怎么恢复？**
> 将对应行的 `delete` 列清空，然后运行 `python run_pipeline.py refresh-plan`，系统会自动将该项恢复为常规分类移动（`action=MOVE`），并重新生成目标路径。
